"""
ARRAYS - MEDIUM 1: Rotate Array by K Positions
Hint: reverse three sections — full array, first k, then the rest

Given a list, rotate it to the right by k steps in-place.
k is non-negative. If k > len(arr), wrap around.

Examples:
  rotate_array([1,2,3,4,5,6,7], 3) -> [5,6,7,1,2,3,4]
  rotate_array([1,2], 3)           -> [2,1]
"""

# ---- YOUR CODE HERE ----

