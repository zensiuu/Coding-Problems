"""
ARRAYS - HARD: Trapping Rain Water
Hint 1: water at position i = max(0, min(max_left[i], max_right[i]) - height[i])
Hint 2: or use two pointers from both ends

Given an array of non-negative integers representing elevation heights,
compute how much water can be trapped after rain.

Example:
  trap_rain_water([0,1,0,2,1,0,1,3,2,1,2,1]) -> 6
"""

# ---- YOUR CODE HERE ----

