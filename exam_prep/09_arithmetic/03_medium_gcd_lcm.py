"""
ARITHMETIC - MEDIUM 1: GCD and LCM
Hint: GCD via Euclidean algorithm, LCM = a*b // GCD

Write functions gcd(a, b) and lcm(a, b) that return the GCD and LCM
of two integers.

Examples:
  gcd(48, 18) -> 6
  lcm(4, 6)   -> 12
"""

def gcd(a, b):
    pass

def lcm(a, b):
    pass


if __name__ == "__main__":
    assert gcd(48, 18) == 6
    assert gcd(7, 13) == 1
    assert gcd(0, 5) == 5
    assert lcm(4, 6) == 12
    assert lcm(3, 5) == 15
    assert lcm(0, 5) == 0
    print("All tests passed!")
